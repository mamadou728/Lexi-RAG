# Core API & Server
fastapi
uvicorn
python-dotenv

# Database & Vector Store
pymongo             # Standard MongoDB driver (Consider 'motor' later for async)
motor               # Async MongoDB driver
beanie              # ODM for MongoDB with Motor
qdrant-client
certifi             # SSL certificate bundle

# AI & RAG Logic (Your existing stack)
langchain
langchain-openai
langchain-community
pypdf               # For parsing legal PDFs
tiktoken            # For counting tokens

# --- NEW SECURITY LAYERS (Required for Architecture) ---

# Layer 2: The Keymaster (Encryption)
cryptography        # REQUIRED for Fernet (AES-256) encryption of text

# Layer 4: RBAC & Auth
passlib[bcrypt]     # REQUIRED to hash user passwords (never store plain text)
bcrypt==3.2.2       # Pin to version compatible with passlib 1.7.4
python-jose[cryptography] # REQUIRED to generate JWT tokens for session handling
python-multipart    # REQUIRED for FastAPI login forms
email-validator      # REQUIRED for Pydantic EmailStr validation